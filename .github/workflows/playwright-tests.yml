name: Playwright Tests

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  playwright-tests:
    name: Run Playwright E2E Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: Install dependencies
        working-directory: extension/Playwright-Trace-Recorder-Crx
        run: npm ci

      - name: Build extension
        working-directory: extension/Playwright-Trace-Recorder-Crx
        run: bash build.sh

      - name: Run Playwright tests
        working-directory: extension/Playwright-Trace-Recorder-Crx/tests
        run: npx playwright test

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: extension/Playwright-Trace-Recorder-Crx/tests/playwright-report/
          retention-days: 30

      - name: Upload trace files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-traces
          path: extension/Playwright-Trace-Recorder-Crx/tests/test-results/
          retention-days: 30
